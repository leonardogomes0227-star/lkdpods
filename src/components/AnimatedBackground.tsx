/**
 * No visual clean, esse fundo com glows neon não faz mais sentido —
 * o layout claro já tem contraste suficiente sem decoração atrás do conteúdo.
 * Mantive o componente (pra não quebrar o import em outros arquivos),
 * só que ele não renderiza nada agora.
 */
export function AnimatedBackground() {
  return null;
}
